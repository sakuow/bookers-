<% if flash[:notice] %>
   <div class="flash">
     <%= flash[:notice] %>
   </div>
 <% end %>

<strong>Title:</strong>
<%= @book.title %></br>
<strong>Body:</strong>
<%= @book.body %></br>
<%= link_to "Edit", edit_book_path(@book.id) %>
<%= link_to "Back", books_path %>
